// Smoke test placeholder. The default Flutter counter test was removed
// because we don't ship a counter UI. Real test coverage (auth flow,
// chat hydrate, etc.) is intentionally deferred — wire when the API
// shape stabilises.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(2 + 2, 4);
  });
}
